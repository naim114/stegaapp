{
  "name": "stega",
  "version": "1.0.0",
  "description": "stega app",
  "main": "main.js",
  "scripts": {
    "start": "concurrently \"npm start --prefix frontend\" \"electron .\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ainisa",
  "license": "ISC",
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
    "@electron-forge/cli": "^7.5.0",
    "concurrently": "^9.1.0",
    "electron": "^33.2.0"
  }
}
